{
  "name": "churrun-tunnel",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/churrun-tunnel",
  "tags": ["type:app", "stack:bash"],
  "targets": {
    "run": {
      "executor": "nx:run-commands",
      "options": { "cwd": "apps/churrun-tunnel", "command": "bash churrun" }
    }
  }
}
